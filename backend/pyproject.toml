[project]
name = "sbd"
version = "0.1.0"
description = "API Backend pour l'analyse de Deadlift avec Gemini"
requires-python = "==3.11.*"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-multipart",
    "google-genai",
    "pydantic",
    "google-auth>=2.56.3",
    "pyjwt>=2.13.0",
]

[tool.uv]
package = false
